# GitHub Copilot adapter

Read/follow root `AGENTS.md`: sole repository instruction source. This adapter
only supports Copilot surfaces that load repository-wide instructions
separately; never duplicate project policy here.
