[project]
name = "neutrinos-repository-validation"
version = "0.0.0"
description = "Repository validation tooling for NeutrinOS"
requires-python = ">=3.14,<3.15"
dependencies = []

[tool.uv]
package = false
