# Point greetd at the configuration in /usr. Its default is /etc/greetd/config.toml,
# and ADR-0004 delivers /etc only by signed confext, so the release artifact
# cannot put a file there.
[Service]
ExecStart=
ExecStart=/usr/bin/greetd --config /usr/lib/neutrinos/greetd.toml
